# Managed by azhp2bk-bootstrap — machine-local chezmoi config (not in dotfiles git)
sourceDir = "{{ workstation_home }}/.local/share/chezmoi"

[data]
    name = "{{ chezmoi_data.name }}"
    email = "{{ chezmoi_data.email }}"
    browser = "{{ chezmoi_data.browser }}"
    desktop = "{{ chezmoi_data.desktop }}"
    os_target = "{{ chezmoi_data.os_target }}"
